# hdx2pdf - dependencias
# Instalar con: pip install -r requirements.txt

# Parser HTML (requerido)
beautifulsoup4>=4.12

# Motor PDF principal (alta fidelidad, HTML→PDF)
# Requiere: libcairo, libpango (ver README para instalación en Linux/Windows)
weasyprint>=60.0

# Motor PDF fallback (sin dependencias del sistema)
reportlab>=4.0

# PDF merging — used to stitch per-chunk WeasyPrint PDFs for large documents
pikepdf>=8.0

# CLI interface
rich>=13.0
InquirerPy>=0.3.4

# Opcional: para extraer texto de HDX binarios/propietarios
# chardet>=5.0
